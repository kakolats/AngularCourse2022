import { FiltreMoneyPipe } from './filtre-money.pipe';

describe('FiltreMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
